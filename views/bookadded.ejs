<!doctype html>
<html>
  <head>
    <title>Book added to <%=shopData.shopName%></title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
  </head>
  <body>
    <h1>Book added to <%=shopData.shopName%></h1>
    <p>Added: <strong><%= addedBook.name %></strong> — £<%= Number(addedBook.price).toFixed(2) %></p>
    <p><a href="/books/list">View all books</a></p>
  </body>
</html>